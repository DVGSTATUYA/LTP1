# 📘 Отчёт по проекту: API мебельной фабрики

## 1. Выбор архитектуры: REST

Мы реализовали **REST-архитектуру**, потому что:
- Она проста в реализации и тестировании (особенно через Postman)
- Поддерживается напрямую FastAPI без дополнительных библиотек
- Удобна для CRUD-операций с таблицами (создание, чтение, обновление, удаление)
- Swagger UI автоматически генерирует документацию для REST-эндпоинтов

GraphQL не использовался, так как он избыточен для текущей задачи и требует отдельной схемы и резолверов.

---

## 2. Почему выбран именно этот стек

| Компонент     | Причина выбора |
|---------------|----------------|
| **FastAPI**   | Современный, быстрый, поддерживает асинхронность, автогенерация Swagger |
| **SQLAlchemy**| Гибкая ORM, позволяет работать с PostgreSQL через Python |
| **PostgreSQL**| Надёжная СУБД, хорошо работает с большими объёмами табличных данных |
| **Pydantic v2**| Валидация входных/выходных данных, строгая типизация |
| **Uvicorn**   | Лёгкий ASGI-сервер для запуска FastAPI-приложений |

---

## 3. Почему выбрана такая архитектура

Мы использовали **модульную архитектуру**, где каждая таблица — это отдельный модуль:

- У каждой сущности есть:
  - SQLAlchemy-модель (`models/`)
  - Pydantic-схемы (`schemas/`)
  - Роутер с эндпоинтами (`routers/`)
- Это упрощает масштабирование, поддержку и повторное использование кода
- Такой подход соответствует принципам **чистой архитектуры** и **разделения ответственности**

---

## 4. Как писались модули и что применялось

- **Модели** создавались на основе структуры Excel-файлов, с учётом оригинальных названий колонок
- Использовались `Identity(start=1)` для автоинкремента `id` в PostgreSQL
- **Схемы** писались через Pydantic v2 с `from_attributes = True`
- **Роутеры** реализованы с базовыми `GET` и `POST` методами
- Все роутеры подключены в `main.py` через `include_router`
- Swagger UI позволяет протестировать API без Postman
- **Тестирование** проводилось через Postman (см. скриншоты)

---

## 5. Скриншоты

_Приложи:_
<img width="1906" height="926" alt="image" src="https://github.com/user-attachments/assets/431c11e5-5e89-4aad-8c0f-5ef740ab1dfd" />

<img width="1275" height="794" alt="image" src="https://github.com/user-attachments/assets/6354cb3c-e00a-4841-bd49-7f5684656e76" />

<img width="303" height="675" alt="image" src="https://github.com/user-attachments/assets/84be3445-1fbe-4b01-8b4d-31212289cc94" />
